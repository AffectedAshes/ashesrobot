// yesdude.js

// Function called when the "yes dude" command is issued
function yesDudeCommand(target, username, client, userMsg, context) {
    client.say(target, `rubent15YO rubent15EZ`);
}

module.exports = {
    yesDudeCommand,
};