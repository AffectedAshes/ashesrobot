// protein.js

// Function called when the "protein" command is issued
function proteinCommand(target, username, client, userMsg, context) {
    client.say(target, `Goditem`);
}

module.exports = {
    proteinCommand,
};